---
import { firm } from '../i18n/ui';

interface Props {
  href: string;
  tone?: 'dark' | 'light';
  size?: 'sm' | 'md';
}
const { href, tone = 'dark', size = 'sm' } = Astro.props;
---

<a class:list={['logo', `logo--${tone}`, `logo--${size}`]} href={href} aria-label={firm.name}>
  <svg class="logo__mark" viewBox="0 0 56 100" aria-hidden="true" focusable="false">
    <defs>
      <linearGradient id="logoMarkSilver" x1="0" y1="0" x2="1" y2="1">
        <stop offset="0%" stop-color="#cfd6dc" />
        <stop offset="45%" stop-color="#9aa5b1" />
        <stop offset="100%" stop-color="#75808d" />
      </linearGradient>
    </defs>
    <rect x="6" y="4" width="3.4" height="92" fill="currentColor" />
    <polygon points="40,4 54,4 30,96 16,96" fill="url(#logoMarkSilver)" />
  </svg>
  <span class="logo__name">{firm.name}</span>
</a>

<style>
  .logo {
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    color: var(--c-primary);
  }
  .logo:hover {
    color: var(--c-primary);
  }
  .logo__mark {
    display: block;
    width: auto;
    height: 3.7rem;
    flex-shrink: 0;
  }
  .logo__name {
    font-family: var(--font-serif);
    font-size: 1.2rem;
    letter-spacing: 0.02em;
    color: inherit;
  }

  .logo--md .logo__mark {
    height: 4.4rem;
  }
  .logo--md .logo__name {
    font-size: 1.4rem;
  }

  .logo--light,
  .logo--light:hover {
    color: #fff;
  }

  @media (max-width: 420px) {
    .logo__name {
      font-size: 1.05rem;
    }
  }
</style>
