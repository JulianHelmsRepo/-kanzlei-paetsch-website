# Kontaktformular (siehe netlify/functions/contact.js)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Deutschen Blog-Bereich live schalten (Standard: aus).
# Auf "true" setzen (lokal in .env, auf Netlify als Environment Variable),
# sobald /blog/de veröffentlicht werden soll.
SHOW_DE_BLOG=false
